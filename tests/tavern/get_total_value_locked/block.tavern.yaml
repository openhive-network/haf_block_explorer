---
test_name: Hafbe PostgREST 

marks:
  - patterntest

includes:
  - !include ../common.yaml

stages:
  - name: get_total_value_locked
    request:
      url: "{service.proto:s}://{service.server:s}:{service.port}/rpc/get_total_value_locked"
      method: POST
      headers:
        content-type: application/json
        accept: application/json
      json: {}   
    response:
      status_code: 200
      verify_response_with:
        function: validate_response:compare_rest_response_with_pattern
